<h3>Arrival</h3>
<%= form_for 'arrival', url: arrival_path do |f| %>
  <% @products.each do |product| %>
    <div>
      <%= f.label product.code %>
      <%= f.text_field product.id, value: 0 %>
      <%= content_tag :span, product.unit %>
    </div>
  <% end %>
  <%= f.submit "Arrived" %>
<% end %>
